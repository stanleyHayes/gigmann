# openapi-typescript still declares a typescript@^5 peer while we run the latest
# TypeScript 6 (it generates .d.ts text and does not use the consumer's tsc at
# codegen time, so this is safe). Keep installs reproducible in CI.
legacy-peer-deps=true
